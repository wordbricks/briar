## 로컬 CI 결과 (local CI)

`bun run ci:local` 통과 (모든 컨텍스트: app-worker, d1-migrations, rust, security).

### 실행 방식
- 경로에 등록된 `bun`이 Briar.app 내장 바이너리(체크섬 불일치)였으므로, `mise`가 관리하는 공식 Bun 1.3.14(`~/.local/share/mise/installs/bun/1.3.14/bin/bun`)를 PATH에 우선해 실행했습니다.
- `runtime:prepare`의 번들 Bun 체크섬 검증이 통과하며 이번 실행은 실패 없이 진행됐습니다.

### 확인된 결과
- shared build inputs: `runtime:prepare`(Bun sidecar 체크섬 일치), `cli:build`, `agent:build` 통과.
- app-worker: `check`, `test`, iOS 릴리스 검증(`ios:release:verify`), `build`, `build:release`, `worker:check/build/startup` 통과.
- d1-migrations: 마이그레이션 로컬 실행 및 DB 테스트 통과.
- rust: rustfmt/clippy(`-D warnings`) 및 cargo test 통과.
- security: cargo-audit(허용 경고 17건만), gitleaks(1400 커밋 스캔, 누출 없음), 암호화 env 검증 통과.
- 최종 출력: "[local-ci] All selected checks passed."
