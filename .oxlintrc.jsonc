{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "jsPlugins": [
    { "name": "@effect", "specifier": "@effect/eslint-plugin" },
    {
      "name": "anti-slop",
      "specifier": "./tools/oxlint/anti-slop/index.ts",
    },
    {
      "name": "anti-slop-effect",
      "specifier": "./tools/oxlint/anti-slop/effect/index.ts",
    },
  ],
  "categories": {
    "correctness": "off",
  },
  "rules": {
    "@effect/no-import-from-barrel-package": [
      "error",
      { "packageNames": ["effect"] },
    ],
    // Void callbacks are idiomatic in Effect, Match, finalizers, and tests.
    "typescript/no-confusing-void-expression": "off",
    // Explicit function-boundary types prevent semantic lint from widening
    // defaulted parameters to `any`; inference brevity is lower priority.
    "typescript/no-inferrable-types": "off",
    // Effect.gen legitimately mixes `return yield*` failures with implicit void success.
    "typescript/consistent-return": "off",
    // Explicit Effect error and requirement channels document API contracts.
    "typescript/no-unnecessary-type-arguments": "off",
    // Anti-slop baseline at b2907815. Re-enable each rule after its count reaches zero.
    // 113 existing findings.
    "anti-slop/no-chained-type-assertions": "off",
    // 90 existing findings.
    "anti-slop/no-conditional-empty-object-spread": "off",
    "anti-slop/no-known-value-widening": "error",
    "anti-slop/no-module-mocking": "error",
    "anti-slop/no-object-parameters": "error",
    "anti-slop/no-reflect-apply": "error",
    "anti-slop/no-reflect-get": "error",
    // 603 existing findings.
    "anti-slop/no-runtime-typeof": "off",
    "anti-slop/no-shape-in-symbol-names": "error",
    // 245 existing findings.
    "anti-slop/no-unknown-parameters": "off",
    // 82 existing findings.
    "anti-slop/no-unknown-returns": "off",
    "anti-slop/no-unknown-type-aliases": "error",
    // 288 existing findings.
    "anti-slop/no-unsafe-dictionary-type": "off",
    "anti-slop/no-widen-then-assert": "error",
    // 899 existing findings.
    "anti-slop/require-safety-comment-for-type-assertion": "off",
    "anti-slop-effect/no-service-constructor-imports": "error",
  },
  "ignorePatterns": [
    "**/node_modules/**",
    "**/dist/**",
    "**/.next/**",
    "**/.wrangler/**",
    "**/target/**",
    ".agent/**",
    ".agents/**",
    ".claude/**",
    ".codex/**",
    ".continue/**",
    ".cursor/**",
    ".gemini/**",
    ".opencode/**",
    ".pi/**",
    ".roo/**",
    ".windsurf/**",
    "tools/oxlint/anti-slop/**",
  ],
}
