# Cloudflare Worker URL, for example:
# https://briar-api.<account-subdomain>.workers.dev
# Local Wrangler: http://127.0.0.1:8787
VITE_BRIAR_API_URL=

# Keep the dashboard usable before a remote Worker is linked.
VITE_BRIAR_DEMO=true
