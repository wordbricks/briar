// @generated by connectrpc-codegen. DO NOT EDIT.

///Shorthand for `OwnedView<ClaimWorkRequestView<'static>>`.
pub type OwnedClaimWorkRequestView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ClaimWorkRequestView<'static>,
>;
///Shorthand for `OwnedView<ClaimWorkResponseView<'static>>`.
pub type OwnedClaimWorkResponseView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ClaimWorkResponseView<'static>,
>;
///Shorthand for `OwnedView<RenewWorkLeaseRequestView<'static>>`.
pub type OwnedRenewWorkLeaseRequestView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::RenewWorkLeaseRequestView<'static>,
>;
///Shorthand for `OwnedView<RenewWorkLeaseResponseView<'static>>`.
pub type OwnedRenewWorkLeaseResponseView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::RenewWorkLeaseResponseView<'static>,
>;
///Shorthand for `OwnedView<HandoffWorkRequestView<'static>>`.
pub type OwnedHandoffWorkRequestView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::HandoffWorkRequestView<'static>,
>;
///Shorthand for `OwnedView<HandoffWorkResponseView<'static>>`.
pub type OwnedHandoffWorkResponseView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::HandoffWorkResponseView<'static>,
>;
///Shorthand for `OwnedView<ClaimIssueRequestView<'static>>`.
pub type OwnedClaimIssueRequestView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ClaimIssueRequestView<'static>,
>;
///Shorthand for `OwnedView<ClaimIssueResponseView<'static>>`.
pub type OwnedClaimIssueResponseView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ClaimIssueResponseView<'static>,
>;
///Shorthand for `OwnedView<ListRunEvidenceRequestView<'static>>`.
pub type OwnedListRunEvidenceRequestView = ::buffa::view::OwnedView<
    crate::proto::briar::app::v1::__buffa::view::ListRunEvidenceRequestView<'static>,
>;
///Shorthand for `OwnedView<ListRunEvidenceResponseView<'static>>`.
pub type OwnedListRunEvidenceResponseView = ::buffa::view::OwnedView<
    crate::proto::briar::app::v1::__buffa::view::ListRunEvidenceResponseView<'static>,
>;
///Shorthand for `OwnedView<AppendTranscriptEventsRequestView<'static>>`.
pub type OwnedAppendTranscriptEventsRequestView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::AppendTranscriptEventsRequestView<
        'static,
    >,
>;
///Shorthand for `OwnedView<AppendTranscriptEventsResponseView<'static>>`.
pub type OwnedAppendTranscriptEventsResponseView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::AppendTranscriptEventsResponseView<
        'static,
    >,
>;
///Shorthand for `OwnedView<ReportIssueExecutionTelemetryRequestView<'static>>`.
pub type OwnedReportIssueExecutionTelemetryRequestView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ReportIssueExecutionTelemetryRequestView<
        'static,
    >,
>;
///Shorthand for `OwnedView<ReportIssueExecutionTelemetryResponseView<'static>>`.
pub type OwnedReportIssueExecutionTelemetryResponseView = ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ReportIssueExecutionTelemetryResponseView<
        'static,
    >,
>;
impl ::connectrpc::Encodable<crate::proto::briar::worker::v1::ClaimWorkResponse>
for crate::proto::briar::worker::v1::__buffa::view::ClaimWorkResponseView<'_> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self, codec)
    }
}
impl ::connectrpc::Encodable<crate::proto::briar::worker::v1::ClaimWorkResponse>
for ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ClaimWorkResponseView<'static>,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self.reborrow(), codec)
    }
    /// An `OwnedView` still holds the buffer it was decoded from, so
    /// its large fields can be handed to the response body by
    /// reference count instead of copied. The bare view impl above
    /// cannot do this: it has borrows but no buffer to name.
    fn encode_segments(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::connectrpc::EncodedBody, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body_segments(
            self.reborrow(),
            self.bytes(),
            codec,
        )
    }
}
impl ::connectrpc::Encodable<crate::proto::briar::worker::v1::RenewWorkLeaseResponse>
for crate::proto::briar::worker::v1::__buffa::view::RenewWorkLeaseResponseView<'_> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self, codec)
    }
}
impl ::connectrpc::Encodable<crate::proto::briar::worker::v1::RenewWorkLeaseResponse>
for ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::RenewWorkLeaseResponseView<'static>,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self.reborrow(), codec)
    }
    /// An `OwnedView` still holds the buffer it was decoded from, so
    /// its large fields can be handed to the response body by
    /// reference count instead of copied. The bare view impl above
    /// cannot do this: it has borrows but no buffer to name.
    fn encode_segments(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::connectrpc::EncodedBody, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body_segments(
            self.reborrow(),
            self.bytes(),
            codec,
        )
    }
}
impl ::connectrpc::Encodable<crate::proto::briar::worker::v1::HandoffWorkResponse>
for crate::proto::briar::worker::v1::__buffa::view::HandoffWorkResponseView<'_> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self, codec)
    }
}
impl ::connectrpc::Encodable<crate::proto::briar::worker::v1::HandoffWorkResponse>
for ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::HandoffWorkResponseView<'static>,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self.reborrow(), codec)
    }
    /// An `OwnedView` still holds the buffer it was decoded from, so
    /// its large fields can be handed to the response body by
    /// reference count instead of copied. The bare view impl above
    /// cannot do this: it has borrows but no buffer to name.
    fn encode_segments(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::connectrpc::EncodedBody, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body_segments(
            self.reborrow(),
            self.bytes(),
            codec,
        )
    }
}
impl ::connectrpc::Encodable<crate::proto::briar::worker::v1::ClaimIssueResponse>
for crate::proto::briar::worker::v1::__buffa::view::ClaimIssueResponseView<'_> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self, codec)
    }
}
impl ::connectrpc::Encodable<crate::proto::briar::worker::v1::ClaimIssueResponse>
for ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ClaimIssueResponseView<'static>,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self.reborrow(), codec)
    }
    /// An `OwnedView` still holds the buffer it was decoded from, so
    /// its large fields can be handed to the response body by
    /// reference count instead of copied. The bare view impl above
    /// cannot do this: it has borrows but no buffer to name.
    fn encode_segments(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::connectrpc::EncodedBody, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body_segments(
            self.reborrow(),
            self.bytes(),
            codec,
        )
    }
}
impl ::connectrpc::Encodable<
    crate::proto::briar::worker::v1::AppendTranscriptEventsResponse,
>
for crate::proto::briar::worker::v1::__buffa::view::AppendTranscriptEventsResponseView<
    '_,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self, codec)
    }
}
impl ::connectrpc::Encodable<
    crate::proto::briar::worker::v1::AppendTranscriptEventsResponse,
>
for ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::AppendTranscriptEventsResponseView<
        'static,
    >,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self.reborrow(), codec)
    }
    /// An `OwnedView` still holds the buffer it was decoded from, so
    /// its large fields can be handed to the response body by
    /// reference count instead of copied. The bare view impl above
    /// cannot do this: it has borrows but no buffer to name.
    fn encode_segments(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::connectrpc::EncodedBody, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body_segments(
            self.reborrow(),
            self.bytes(),
            codec,
        )
    }
}
impl ::connectrpc::Encodable<
    crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryResponse,
>
for crate::proto::briar::worker::v1::__buffa::view::ReportIssueExecutionTelemetryResponseView<
    '_,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self, codec)
    }
}
impl ::connectrpc::Encodable<
    crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryResponse,
>
for ::buffa::view::OwnedView<
    crate::proto::briar::worker::v1::__buffa::view::ReportIssueExecutionTelemetryResponseView<
        'static,
    >,
> {
    fn encode(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::buffa::bytes::Bytes, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body(self.reborrow(), codec)
    }
    /// An `OwnedView` still holds the buffer it was decoded from, so
    /// its large fields can be handed to the response body by
    /// reference count instead of copied. The bare view impl above
    /// cannot do this: it has borrows but no buffer to name.
    fn encode_segments(
        &self,
        codec: ::connectrpc::CodecFormat,
    ) -> ::std::result::Result<::connectrpc::EncodedBody, ::connectrpc::ConnectError> {
        ::connectrpc::__codegen::encode_view_body_segments(
            self.reborrow(),
            self.bytes(),
            codec,
        )
    }
}
/// Full service name for this service.
pub const WORKER_QUEUE_SERVICE_SERVICE_NAME: &str = "briar.worker.v1.WorkerQueueService";
/// Static [`Spec`](::connectrpc::Spec) for the `ClaimWork` RPC, as seen by the server; the generated client passes it with [`origin`](::connectrpc::Spec::origin) `Client` (compare across sides with [`Spec::same_method`](::connectrpc::Spec::same_method)).
pub const WORKER_QUEUE_SERVICE_CLAIM_WORK_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/briar.worker.v1.WorkerQueueService/ClaimWork",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// Static [`Spec`](::connectrpc::Spec) for the `RenewWorkLease` RPC, as seen by the server; the generated client passes it with [`origin`](::connectrpc::Spec::origin) `Client` (compare across sides with [`Spec::same_method`](::connectrpc::Spec::same_method)).
pub const WORKER_QUEUE_SERVICE_RENEW_WORK_LEASE_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/briar.worker.v1.WorkerQueueService/RenewWorkLease",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// Static [`Spec`](::connectrpc::Spec) for the `HandoffWork` RPC, as seen by the server; the generated client passes it with [`origin`](::connectrpc::Spec::origin) `Client` (compare across sides with [`Spec::same_method`](::connectrpc::Spec::same_method)).
pub const WORKER_QUEUE_SERVICE_HANDOFF_WORK_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/briar.worker.v1.WorkerQueueService/HandoffWork",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// The machine Worker queue control plane. Large result uploads, transcripts,
/// evidence, and provider-specific execution endpoints intentionally remain
/// outside this service because they have different lifecycle and transport
/// requirements.
///
/// # Implementing handlers
///
/// Implement methods with plain `async fn`; the returned future satisfies
/// the `Send` bound automatically.
///
/// **Unary and server-streaming requests** arrive as
/// [`ServiceRequest<'_, Req>`](::connectrpc::ServiceRequest): a zero-copy
/// view of the request plus its body, valid for the duration of the call.
/// Fields are read directly (`request.name` is a `&str` into the decoded
/// buffer) and the borrow may be held across `.await` points. Anything
/// that must outlive the call — `tokio::spawn`, channels, server state,
/// or data captured by a returned response stream — takes owned data:
/// call `request.to_owned_message()` (or copy the specific fields)
/// first.
///
/// **Client-streaming and bidi requests** arrive as
/// [`InboundStream<Req>`](::connectrpc::InboundStream) — a
/// `ServiceStream` of [`StreamMessage`](::connectrpc::StreamMessage)s.
/// Each item owns its decoded buffer and is `Send + 'static`, so items
/// can be buffered or moved into spawned tasks; read fields zero-copy
/// through the generated accessor methods (`item.name()`) or `.view()`,
/// convert with `.to_owned_message()`, or yield an item back unchanged —
/// `StreamMessage<M>` implements `Encodable<M>`.
///
/// Request types resolved through `extern_path` (e.g. well-known types
/// from another crate) use the same wrappers; the crate that owns the
/// type must be generated with buffa ≥ 0.9.0 and views enabled so the
/// backing `HasMessageView` impl exists.
///
/// The `impl Encodable<Out>` return bound accepts the owned `Out`, the
/// generated `OutView<'_>` / `OwnedOutView`,
/// [`MaybeBorrowed`](::connectrpc::MaybeBorrowed), or
/// [`PreEncoded`](::connectrpc::PreEncoded) for handlers that encode a
/// non-`'static` view internally and pass the bytes across the handler
/// boundary. View bodies are not emitted for output types mapped via
/// `extern_path` (the impl would be an orphan); return owned for
/// WKT/extern outputs.
///
/// Server-streaming and bidi-streaming methods return
/// `ServiceStream<impl Encodable<Out> + Send + use<Self>>`. The
/// `use<Self>` precise-capturing clause excludes `&self`'s lifetime and
/// the request's lifetime (unary methods use `use<'a, Self>` and may
/// borrow from `&self`), so stream items must be `'static` and cannot
/// borrow from the request. To stream view-encoded data, encode each
/// item inside the stream body and yield
/// [`PreEncoded`](::connectrpc::PreEncoded) — see its `# Streaming
/// example` doc.
#[allow(clippy::type_complexity)]
pub trait WorkerQueueService: Send + Sync + 'static {
    /// Handle the ClaimWork RPC.
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn claim_work<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::briar::worker::v1::ClaimWorkRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::briar::worker::v1::ClaimWorkResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
    /// Handle the RenewWorkLease RPC.
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn renew_work_lease<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::briar::worker::v1::RenewWorkLeaseRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::briar::worker::v1::RenewWorkLeaseResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
    /// Handle the HandoffWork RPC.
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn handoff_work<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::briar::worker::v1::HandoffWorkRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::briar::worker::v1::HandoffWorkResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
}
/// Extension trait for registering a service implementation with a Router.
///
/// This trait is automatically implemented for all types that implement the service trait.
/// Prefer [`Router::add_service`](::connectrpc::Router::add_service) for
/// top-down registration; `register` remains available for compatibility
/// and cases where the service-first call shape is more convenient.
///
/// # Example
///
/// ```rust,ignore
/// use std::sync::Arc;
///
/// let service = Arc::new(MyServiceImpl);
/// let router = service.register(Router::new());
/// ```
pub trait WorkerQueueServiceExt: WorkerQueueService {
    /// Register this service implementation with a Router.
    ///
    /// Takes ownership of the `Arc<Self>` and returns a new Router with
    /// this service's methods registered.
    fn register(
        self: ::std::sync::Arc<Self>,
        router: ::connectrpc::Router,
    ) -> ::connectrpc::Router;
}
impl<S: WorkerQueueService> WorkerQueueServiceExt for S {
    fn register(
        self: ::std::sync::Arc<Self>,
        router: ::connectrpc::Router,
    ) -> ::connectrpc::Router {
        router
            .route_view(
                WORKER_QUEUE_SERVICE_SERVICE_NAME,
                "ClaimWork",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::briar::worker::v1::__buffa::view::ClaimWorkRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::briar::worker::v1::ClaimWorkRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.claim_work(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::briar::worker::v1::ClaimWorkResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(WORKER_QUEUE_SERVICE_CLAIM_WORK_SPEC)
            .route_view(
                WORKER_QUEUE_SERVICE_SERVICE_NAME,
                "RenewWorkLease",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::briar::worker::v1::__buffa::view::RenewWorkLeaseRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::briar::worker::v1::RenewWorkLeaseRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.renew_work_lease(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::briar::worker::v1::RenewWorkLeaseResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(WORKER_QUEUE_SERVICE_RENEW_WORK_LEASE_SPEC)
            .route_view(
                WORKER_QUEUE_SERVICE_SERVICE_NAME,
                "HandoffWork",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::briar::worker::v1::__buffa::view::HandoffWorkRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::briar::worker::v1::HandoffWorkRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.handoff_work(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::briar::worker::v1::HandoffWorkResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(WORKER_QUEUE_SERVICE_HANDOFF_WORK_SPEC)
    }
}
/// Type-inference marker used by [`Router::add_service`](::connectrpc::Router::add_service).
#[doc(hidden)]
pub struct WorkerQueueServiceRegisterMarker;
impl<
    S: WorkerQueueService,
> ::connectrpc::ServiceRegister<WorkerQueueServiceRegisterMarker>
for ::std::sync::Arc<S> {
    fn register_service(self, router: ::connectrpc::Router) -> ::connectrpc::Router {
        <S as WorkerQueueServiceExt>::register(self, router)
    }
}
/// Monomorphic dispatcher for `WorkerQueueService`.
///
/// Unlike `.register(Router)` which type-erases each method into an `Arc<dyn ErasedHandler>` stored in a `HashMap`, this struct dispatches via a compile-time `match` on method name: no vtable, no hash lookup.
///
/// # Example
///
/// ```rust,ignore
/// use connectrpc::ConnectRpcService;
///
/// let server = WorkerQueueServiceServer::new(MyImpl);
/// let service = ConnectRpcService::new(server);
/// // hand `service` to axum/hyper as a fallback_service
/// ```
pub struct WorkerQueueServiceServer<T> {
    inner: ::std::sync::Arc<T>,
}
impl<T: WorkerQueueService> WorkerQueueServiceServer<T> {
    /// Wrap a service implementation in a monomorphic dispatcher.
    pub fn new(service: T) -> Self {
        Self {
            inner: ::std::sync::Arc::new(service),
        }
    }
    /// Wrap an already-`Arc`'d service implementation.
    pub fn from_arc(inner: ::std::sync::Arc<T>) -> Self {
        Self { inner }
    }
}
impl<T> Clone for WorkerQueueServiceServer<T> {
    fn clone(&self) -> Self {
        Self {
            inner: ::std::sync::Arc::clone(&self.inner),
        }
    }
}
impl<T: WorkerQueueService> ::connectrpc::Dispatcher for WorkerQueueServiceServer<T> {
    #[inline]
    fn lookup(
        &self,
        path: &str,
    ) -> Option<::connectrpc::dispatcher::codegen::MethodDescriptor> {
        let method = path.strip_prefix("briar.worker.v1.WorkerQueueService/")?;
        match method {
            "ClaimWork" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(WORKER_QUEUE_SERVICE_CLAIM_WORK_SPEC),
                )
            }
            "RenewWorkLease" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(WORKER_QUEUE_SERVICE_RENEW_WORK_LEASE_SPEC),
                )
            }
            "HandoffWork" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(WORKER_QUEUE_SERVICE_HANDOFF_WORK_SPEC),
                )
            }
            _ => None,
        }
    }
    fn call_unary(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::Payload,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::UnaryResult {
        let Some(method) = path.strip_prefix("briar.worker.v1.WorkerQueueService/") else {
            return ::connectrpc::dispatcher::codegen::unimplemented_unary(path);
        };
        let _ = (&ctx, &request, &format);
        match method {
            "ClaimWork" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::briar::worker::v1::ClaimWorkRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::briar::worker::v1::__buffa::view::ClaimWorkRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                        ctx.decode_options(),
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::briar::worker::v1::ClaimWorkRequest,
                    >::from_parts(&req, &body);
                    svc.claim_work(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::briar::worker::v1::ClaimWorkResponse,
                        >(format)
                })
            }
            "RenewWorkLease" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::briar::worker::v1::RenewWorkLeaseRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::briar::worker::v1::__buffa::view::RenewWorkLeaseRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                        ctx.decode_options(),
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::briar::worker::v1::RenewWorkLeaseRequest,
                    >::from_parts(&req, &body);
                    svc.renew_work_lease(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::briar::worker::v1::RenewWorkLeaseResponse,
                        >(format)
                })
            }
            "HandoffWork" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::briar::worker::v1::HandoffWorkRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::briar::worker::v1::__buffa::view::HandoffWorkRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                        ctx.decode_options(),
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::briar::worker::v1::HandoffWorkRequest,
                    >::from_parts(&req, &body);
                    svc.handoff_work(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::briar::worker::v1::HandoffWorkResponse,
                        >(format)
                })
            }
            _ => ::connectrpc::dispatcher::codegen::unimplemented_unary(path),
        }
    }
    fn call_server_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        request: ::buffa::bytes::Bytes,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::StreamingResult {
        let Some(method) = path.strip_prefix("briar.worker.v1.WorkerQueueService/") else {
            return ::connectrpc::dispatcher::codegen::unimplemented_streaming(path);
        };
        let _ = (&ctx, &request, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_streaming(path),
        }
    }
    fn call_client_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        requests: ::connectrpc::dispatcher::codegen::RequestStream,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::UnaryResult {
        let Some(method) = path.strip_prefix("briar.worker.v1.WorkerQueueService/") else {
            return ::connectrpc::dispatcher::codegen::unimplemented_unary(path);
        };
        let _ = (&ctx, &requests, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_unary(path),
        }
    }
    fn call_bidi_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        requests: ::connectrpc::dispatcher::codegen::RequestStream,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::StreamingResult {
        let Some(method) = path.strip_prefix("briar.worker.v1.WorkerQueueService/") else {
            return ::connectrpc::dispatcher::codegen::unimplemented_streaming(path);
        };
        let _ = (&ctx, &requests, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_streaming(path),
        }
    }
}
/// Client for this service.
///
/// Generic over `T: ClientTransport`. For **gRPC** (HTTP/2), use
/// `Http2Connection` — it has honest `poll_ready` and composes with
/// `tower::balance` for multi-connection load balancing. For **Connect
/// over HTTP/1.1** (or unknown protocol), use `HttpClient`.
///
/// # Example (gRPC / HTTP/2)
///
/// ```rust,ignore
/// use connectrpc::client::{Http2Connection, ClientConfig};
/// use connectrpc::Protocol;
///
/// let uri: http::Uri = "http://localhost:8080".parse()?;
/// let conn = Http2Connection::connect_plaintext(uri.clone()).await?.shared(1024);
/// let config = ClientConfig::new(uri).with_protocol(Protocol::Grpc);
///
/// let client = WorkerQueueServiceClient::new(conn, config);
/// let response = client.claim_work(request).await?;
/// ```
///
/// # Example (Connect / HTTP/1.1 or ALPN)
///
/// ```rust,ignore
/// use connectrpc::client::{HttpClient, ClientConfig};
///
/// let http = HttpClient::plaintext();  // cleartext http:// only
/// let config = ClientConfig::new("http://localhost:8080".parse()?);
///
/// let client = WorkerQueueServiceClient::new(http, config);
/// let response = client.claim_work(request).await?;
/// ```
///
/// # Working with the response
///
/// Unary calls return [`UnaryResponse<OwnedView<FooView>>`](::connectrpc::client::UnaryResponse).
/// [`view()`](::connectrpc::client::UnaryResponse::view) borrows the response
/// message, so field access is zero-copy:
///
/// ```rust,ignore
/// let resp = client.claim_work(request).await?;
/// let name: &str = resp.view().name;  // borrow into the response buffer
/// ```
///
/// If you need the owned struct (e.g. to store or pass by value), use
/// [`into_owned()`](::connectrpc::client::UnaryResponse::into_owned):
///
/// ```rust,ignore
/// let owned = client.claim_work(request).await?.into_owned();
/// ```
///
/// [`into_view()`](::connectrpc::client::UnaryResponse::into_view) keeps the
/// zero-copy decoded body (an `OwnedView`) without copying; field access on it
/// goes through `.reborrow()`. Streaming responses yield one
/// [`StreamMessage`](::connectrpc::StreamMessage) per received message from
/// `.message().await` — read fields zero-copy through the generated accessor
/// methods (`msg.name()`) or `.view()`, or convert with `.to_owned_message()`.
#[cfg(feature = "client")]
#[derive(Clone)]
pub struct WorkerQueueServiceClient<T> {
    transport: T,
    config: ::connectrpc::client::ClientConfig,
}
#[cfg(feature = "client")]
impl<T> WorkerQueueServiceClient<T>
where
    T: ::connectrpc::client::ClientTransport,
    <T::ResponseBody as ::connectrpc::http_body::Body>::Error: ::std::fmt::Display,
{
    /// Create a new client with the given transport and configuration.
    pub fn new(transport: T, config: ::connectrpc::client::ClientConfig) -> Self {
        Self { transport, config }
    }
    /// Get the client configuration.
    pub fn config(&self) -> &::connectrpc::client::ClientConfig {
        &self.config
    }
    /// Get a mutable reference to the client configuration.
    pub fn config_mut(&mut self) -> &mut ::connectrpc::client::ClientConfig {
        &mut self.config
    }
    /// Call the ClaimWork RPC. Sends a request to /briar.worker.v1.WorkerQueueService/ClaimWork.
    pub async fn claim_work(
        &self,
        request: crate::proto::briar::worker::v1::ClaimWorkRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::ClaimWorkResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.claim_work_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the ClaimWork RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn claim_work_with_options(
        &self,
        request: crate::proto::briar::worker::v1::ClaimWorkRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::ClaimWorkResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                WORKER_QUEUE_SERVICE_CLAIM_WORK_SPEC
                    .with_origin(::connectrpc::SpecOrigin::Client),
                request,
                options,
            )
            .await
    }
    /// Call the RenewWorkLease RPC. Sends a request to /briar.worker.v1.WorkerQueueService/RenewWorkLease.
    pub async fn renew_work_lease(
        &self,
        request: crate::proto::briar::worker::v1::RenewWorkLeaseRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::RenewWorkLeaseResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.renew_work_lease_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the RenewWorkLease RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn renew_work_lease_with_options(
        &self,
        request: crate::proto::briar::worker::v1::RenewWorkLeaseRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::RenewWorkLeaseResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                WORKER_QUEUE_SERVICE_RENEW_WORK_LEASE_SPEC
                    .with_origin(::connectrpc::SpecOrigin::Client),
                request,
                options,
            )
            .await
    }
    /// Call the HandoffWork RPC. Sends a request to /briar.worker.v1.WorkerQueueService/HandoffWork.
    pub async fn handoff_work(
        &self,
        request: crate::proto::briar::worker::v1::HandoffWorkRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::HandoffWorkResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.handoff_work_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the HandoffWork RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn handoff_work_with_options(
        &self,
        request: crate::proto::briar::worker::v1::HandoffWorkRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::HandoffWorkResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                WORKER_QUEUE_SERVICE_HANDOFF_WORK_SPEC
                    .with_origin(::connectrpc::SpecOrigin::Client),
                request,
                options,
            )
            .await
    }
}
/// Full service name for this service.
pub const WORKER_EXECUTION_SERVICE_SERVICE_NAME: &str = "briar.worker.v1.WorkerExecutionService";
/// Static [`Spec`](::connectrpc::Spec) for the `ClaimIssue` RPC, as seen by the server; the generated client passes it with [`origin`](::connectrpc::Spec::origin) `Client` (compare across sides with [`Spec::same_method`](::connectrpc::Spec::same_method)).
pub const WORKER_EXECUTION_SERVICE_CLAIM_ISSUE_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/briar.worker.v1.WorkerExecutionService/ClaimIssue",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// Static [`Spec`](::connectrpc::Spec) for the `ListRunEvidence` RPC, as seen by the server; the generated client passes it with [`origin`](::connectrpc::Spec::origin) `Client` (compare across sides with [`Spec::same_method`](::connectrpc::Spec::same_method)).
pub const WORKER_EXECUTION_SERVICE_LIST_RUN_EVIDENCE_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/briar.worker.v1.WorkerExecutionService/ListRunEvidence",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// Static [`Spec`](::connectrpc::Spec) for the `AppendTranscriptEvents` RPC, as seen by the server; the generated client passes it with [`origin`](::connectrpc::Spec::origin) `Client` (compare across sides with [`Spec::same_method`](::connectrpc::Spec::same_method)).
pub const WORKER_EXECUTION_SERVICE_APPEND_TRANSCRIPT_EVENTS_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/briar.worker.v1.WorkerExecutionService/AppendTranscriptEvents",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// Static [`Spec`](::connectrpc::Spec) for the `ReportIssueExecutionTelemetry` RPC, as seen by the server; the generated client passes it with [`origin`](::connectrpc::Spec::origin) `Client` (compare across sides with [`Spec::same_method`](::connectrpc::Spec::same_method)).
pub const WORKER_EXECUTION_SERVICE_REPORT_ISSUE_EXECUTION_TELEMETRY_SPEC: ::connectrpc::Spec = ::connectrpc::Spec::server(
        "/briar.worker.v1.WorkerExecutionService/ReportIssueExecutionTelemetry",
        ::connectrpc::StreamType::Unary,
    )
    .with_idempotency_level(::connectrpc::IdempotencyLevel::Unknown);
/// Execution-credential operations shared by the interactive CLI and the
/// desktop-hosted Auto Hunt runtime.
///
/// # Implementing handlers
///
/// Implement methods with plain `async fn`; the returned future satisfies
/// the `Send` bound automatically.
///
/// **Unary and server-streaming requests** arrive as
/// [`ServiceRequest<'_, Req>`](::connectrpc::ServiceRequest): a zero-copy
/// view of the request plus its body, valid for the duration of the call.
/// Fields are read directly (`request.name` is a `&str` into the decoded
/// buffer) and the borrow may be held across `.await` points. Anything
/// that must outlive the call — `tokio::spawn`, channels, server state,
/// or data captured by a returned response stream — takes owned data:
/// call `request.to_owned_message()` (or copy the specific fields)
/// first.
///
/// **Client-streaming and bidi requests** arrive as
/// [`InboundStream<Req>`](::connectrpc::InboundStream) — a
/// `ServiceStream` of [`StreamMessage`](::connectrpc::StreamMessage)s.
/// Each item owns its decoded buffer and is `Send + 'static`, so items
/// can be buffered or moved into spawned tasks; read fields zero-copy
/// through the generated accessor methods (`item.name()`) or `.view()`,
/// convert with `.to_owned_message()`, or yield an item back unchanged —
/// `StreamMessage<M>` implements `Encodable<M>`.
///
/// Request types resolved through `extern_path` (e.g. well-known types
/// from another crate) use the same wrappers; the crate that owns the
/// type must be generated with buffa ≥ 0.9.0 and views enabled so the
/// backing `HasMessageView` impl exists.
///
/// The `impl Encodable<Out>` return bound accepts the owned `Out`, the
/// generated `OutView<'_>` / `OwnedOutView`,
/// [`MaybeBorrowed`](::connectrpc::MaybeBorrowed), or
/// [`PreEncoded`](::connectrpc::PreEncoded) for handlers that encode a
/// non-`'static` view internally and pass the bytes across the handler
/// boundary. View bodies are not emitted for output types mapped via
/// `extern_path` (the impl would be an orphan); return owned for
/// WKT/extern outputs.
///
/// Server-streaming and bidi-streaming methods return
/// `ServiceStream<impl Encodable<Out> + Send + use<Self>>`. The
/// `use<Self>` precise-capturing clause excludes `&self`'s lifetime and
/// the request's lifetime (unary methods use `use<'a, Self>` and may
/// borrow from `&self`), so stream items must be `'static` and cannot
/// borrow from the request. To stream view-encoded data, encode each
/// item inside the stream body and yield
/// [`PreEncoded`](::connectrpc::PreEncoded) — see its `# Streaming
/// example` doc.
#[allow(clippy::type_complexity)]
pub trait WorkerExecutionService: Send + Sync + 'static {
    /// Handle the ClaimIssue RPC.
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn claim_issue<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::briar::worker::v1::ClaimIssueRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::briar::worker::v1::ClaimIssueResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
    /// Shared intentionally with IssueService so evidence has one DTO.
    /// buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn list_run_evidence<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::briar::app::v1::ListRunEvidenceRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::briar::app::v1::ListRunEvidenceResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
    /// Handle the AppendTranscriptEvents RPC.
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn append_transcript_events<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::briar::worker::v1::AppendTranscriptEventsRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::briar::worker::v1::AppendTranscriptEventsResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
    /// Handle the ReportIssueExecutionTelemetry RPC.
    ///
    /// `'a` lets the response body borrow from `&self` (e.g. server-resident state).
    ///
    /// `request` is borrowed from the request body and is valid for the
    /// duration of the call; message fields are read directly on it
    /// (zero-copy). The response cannot borrow from `request` — use
    /// `.to_owned_message()` (or copy the specific fields) for anything
    /// returned, stored, or moved into `tokio::spawn`.
    fn report_issue_execution_telemetry<'a>(
        &'a self,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::ServiceRequest<
            '_,
            crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryRequest,
        >,
    ) -> impl ::std::future::Future<
        Output = ::connectrpc::ServiceResult<
            impl ::connectrpc::Encodable<
                crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryResponse,
            > + Send + use<'a, Self>,
        >,
    > + Send;
}
/// Extension trait for registering a service implementation with a Router.
///
/// This trait is automatically implemented for all types that implement the service trait.
/// Prefer [`Router::add_service`](::connectrpc::Router::add_service) for
/// top-down registration; `register` remains available for compatibility
/// and cases where the service-first call shape is more convenient.
///
/// # Example
///
/// ```rust,ignore
/// use std::sync::Arc;
///
/// let service = Arc::new(MyServiceImpl);
/// let router = service.register(Router::new());
/// ```
pub trait WorkerExecutionServiceExt: WorkerExecutionService {
    /// Register this service implementation with a Router.
    ///
    /// Takes ownership of the `Arc<Self>` and returns a new Router with
    /// this service's methods registered.
    fn register(
        self: ::std::sync::Arc<Self>,
        router: ::connectrpc::Router,
    ) -> ::connectrpc::Router;
}
impl<S: WorkerExecutionService> WorkerExecutionServiceExt for S {
    fn register(
        self: ::std::sync::Arc<Self>,
        router: ::connectrpc::Router,
    ) -> ::connectrpc::Router {
        router
            .route_view(
                WORKER_EXECUTION_SERVICE_SERVICE_NAME,
                "ClaimIssue",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::briar::worker::v1::__buffa::view::ClaimIssueRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::briar::worker::v1::ClaimIssueRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.claim_issue(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::briar::worker::v1::ClaimIssueResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(WORKER_EXECUTION_SERVICE_CLAIM_ISSUE_SPEC)
            .route_view(
                WORKER_EXECUTION_SERVICE_SERVICE_NAME,
                "ListRunEvidence",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::briar::app::v1::__buffa::view::ListRunEvidenceRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::briar::app::v1::ListRunEvidenceRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.list_run_evidence(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::briar::app::v1::ListRunEvidenceResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(WORKER_EXECUTION_SERVICE_LIST_RUN_EVIDENCE_SPEC)
            .route_view(
                WORKER_EXECUTION_SERVICE_SERVICE_NAME,
                "AppendTranscriptEvents",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::briar::worker::v1::__buffa::view::AppendTranscriptEventsRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::briar::worker::v1::AppendTranscriptEventsRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.append_transcript_events(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::briar::worker::v1::AppendTranscriptEventsResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(WORKER_EXECUTION_SERVICE_APPEND_TRANSCRIPT_EVENTS_SPEC)
            .route_view(
                WORKER_EXECUTION_SERVICE_SERVICE_NAME,
                "ReportIssueExecutionTelemetry",
                {
                    let svc = ::std::sync::Arc::clone(&self);
                    ::connectrpc::view_handler_fn(move |
                        ctx,
                        req: ::buffa::view::OwnedView<
                            crate::proto::briar::worker::v1::__buffa::view::ReportIssueExecutionTelemetryRequestView<
                                'static,
                            >,
                        >,
                        format|
                    {
                        let svc = ::std::sync::Arc::clone(&svc);
                        async move {
                            let sreq = ::connectrpc::ServiceRequest::<
                                crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryRequest,
                            >::from_parts(req.reborrow(), req.bytes());
                            svc.report_issue_execution_telemetry(ctx, sreq)
                                .await?
                                .encode::<
                                    crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryResponse,
                                >(format)
                        }
                    })
                },
            )
            .with_spec(WORKER_EXECUTION_SERVICE_REPORT_ISSUE_EXECUTION_TELEMETRY_SPEC)
    }
}
/// Type-inference marker used by [`Router::add_service`](::connectrpc::Router::add_service).
#[doc(hidden)]
pub struct WorkerExecutionServiceRegisterMarker;
impl<
    S: WorkerExecutionService,
> ::connectrpc::ServiceRegister<WorkerExecutionServiceRegisterMarker>
for ::std::sync::Arc<S> {
    fn register_service(self, router: ::connectrpc::Router) -> ::connectrpc::Router {
        <S as WorkerExecutionServiceExt>::register(self, router)
    }
}
/// Monomorphic dispatcher for `WorkerExecutionService`.
///
/// Unlike `.register(Router)` which type-erases each method into an `Arc<dyn ErasedHandler>` stored in a `HashMap`, this struct dispatches via a compile-time `match` on method name: no vtable, no hash lookup.
///
/// # Example
///
/// ```rust,ignore
/// use connectrpc::ConnectRpcService;
///
/// let server = WorkerExecutionServiceServer::new(MyImpl);
/// let service = ConnectRpcService::new(server);
/// // hand `service` to axum/hyper as a fallback_service
/// ```
pub struct WorkerExecutionServiceServer<T> {
    inner: ::std::sync::Arc<T>,
}
impl<T: WorkerExecutionService> WorkerExecutionServiceServer<T> {
    /// Wrap a service implementation in a monomorphic dispatcher.
    pub fn new(service: T) -> Self {
        Self {
            inner: ::std::sync::Arc::new(service),
        }
    }
    /// Wrap an already-`Arc`'d service implementation.
    pub fn from_arc(inner: ::std::sync::Arc<T>) -> Self {
        Self { inner }
    }
}
impl<T> Clone for WorkerExecutionServiceServer<T> {
    fn clone(&self) -> Self {
        Self {
            inner: ::std::sync::Arc::clone(&self.inner),
        }
    }
}
impl<T: WorkerExecutionService> ::connectrpc::Dispatcher
for WorkerExecutionServiceServer<T> {
    #[inline]
    fn lookup(
        &self,
        path: &str,
    ) -> Option<::connectrpc::dispatcher::codegen::MethodDescriptor> {
        let method = path.strip_prefix("briar.worker.v1.WorkerExecutionService/")?;
        match method {
            "ClaimIssue" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(WORKER_EXECUTION_SERVICE_CLAIM_ISSUE_SPEC),
                )
            }
            "ListRunEvidence" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(WORKER_EXECUTION_SERVICE_LIST_RUN_EVIDENCE_SPEC),
                )
            }
            "AppendTranscriptEvents" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(
                            WORKER_EXECUTION_SERVICE_APPEND_TRANSCRIPT_EVENTS_SPEC,
                        ),
                )
            }
            "ReportIssueExecutionTelemetry" => {
                Some(
                    ::connectrpc::dispatcher::codegen::MethodDescriptor::unary(false)
                        .with_spec(
                            WORKER_EXECUTION_SERVICE_REPORT_ISSUE_EXECUTION_TELEMETRY_SPEC,
                        ),
                )
            }
            _ => None,
        }
    }
    fn call_unary(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        request: ::connectrpc::Payload,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::UnaryResult {
        let Some(method) = path.strip_prefix("briar.worker.v1.WorkerExecutionService/")
        else {
            return ::connectrpc::dispatcher::codegen::unimplemented_unary(path);
        };
        let _ = (&ctx, &request, &format);
        match method {
            "ClaimIssue" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::briar::worker::v1::ClaimIssueRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::briar::worker::v1::__buffa::view::ClaimIssueRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                        ctx.decode_options(),
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::briar::worker::v1::ClaimIssueRequest,
                    >::from_parts(&req, &body);
                    svc.claim_issue(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::briar::worker::v1::ClaimIssueResponse,
                        >(format)
                })
            }
            "ListRunEvidence" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::briar::app::v1::ListRunEvidenceRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::briar::app::v1::__buffa::view::ListRunEvidenceRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                        ctx.decode_options(),
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::briar::app::v1::ListRunEvidenceRequest,
                    >::from_parts(&req, &body);
                    svc.list_run_evidence(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::briar::app::v1::ListRunEvidenceResponse,
                        >(format)
                })
            }
            "AppendTranscriptEvents" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::briar::worker::v1::AppendTranscriptEventsRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::briar::worker::v1::__buffa::view::AppendTranscriptEventsRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                        ctx.decode_options(),
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::briar::worker::v1::AppendTranscriptEventsRequest,
                    >::from_parts(&req, &body);
                    svc.append_transcript_events(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::briar::worker::v1::AppendTranscriptEventsResponse,
                        >(format)
                })
            }
            "ReportIssueExecutionTelemetry" => {
                let svc = ::std::sync::Arc::clone(&self.inner);
                Box::pin(async move {
                    let body = ::connectrpc::dispatcher::codegen::request_proto_bytes::<
                        crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryRequest,
                    >(request.encoded()?, format)?;
                    let req: crate::proto::briar::worker::v1::__buffa::view::ReportIssueExecutionTelemetryRequestView<
                        '_,
                    > = ::connectrpc::dispatcher::codegen::decode_borrowed_request_view(
                        &body,
                        ctx.decode_options(),
                    )?;
                    let req = ::connectrpc::ServiceRequest::<
                        crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryRequest,
                    >::from_parts(&req, &body);
                    svc.report_issue_execution_telemetry(ctx, req)
                        .await?
                        .encode::<
                            crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryResponse,
                        >(format)
                })
            }
            _ => ::connectrpc::dispatcher::codegen::unimplemented_unary(path),
        }
    }
    fn call_server_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        request: ::buffa::bytes::Bytes,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::StreamingResult {
        let Some(method) = path.strip_prefix("briar.worker.v1.WorkerExecutionService/")
        else {
            return ::connectrpc::dispatcher::codegen::unimplemented_streaming(path);
        };
        let _ = (&ctx, &request, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_streaming(path),
        }
    }
    fn call_client_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        requests: ::connectrpc::dispatcher::codegen::RequestStream,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::UnaryResult {
        let Some(method) = path.strip_prefix("briar.worker.v1.WorkerExecutionService/")
        else {
            return ::connectrpc::dispatcher::codegen::unimplemented_unary(path);
        };
        let _ = (&ctx, &requests, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_unary(path),
        }
    }
    fn call_bidi_streaming(
        &self,
        path: &str,
        ctx: ::connectrpc::RequestContext,
        requests: ::connectrpc::dispatcher::codegen::RequestStream,
        format: ::connectrpc::CodecFormat,
    ) -> ::connectrpc::dispatcher::codegen::StreamingResult {
        let Some(method) = path.strip_prefix("briar.worker.v1.WorkerExecutionService/")
        else {
            return ::connectrpc::dispatcher::codegen::unimplemented_streaming(path);
        };
        let _ = (&ctx, &requests, &format);
        match method {
            _ => ::connectrpc::dispatcher::codegen::unimplemented_streaming(path),
        }
    }
}
/// Client for this service.
///
/// Generic over `T: ClientTransport`. For **gRPC** (HTTP/2), use
/// `Http2Connection` — it has honest `poll_ready` and composes with
/// `tower::balance` for multi-connection load balancing. For **Connect
/// over HTTP/1.1** (or unknown protocol), use `HttpClient`.
///
/// # Example (gRPC / HTTP/2)
///
/// ```rust,ignore
/// use connectrpc::client::{Http2Connection, ClientConfig};
/// use connectrpc::Protocol;
///
/// let uri: http::Uri = "http://localhost:8080".parse()?;
/// let conn = Http2Connection::connect_plaintext(uri.clone()).await?.shared(1024);
/// let config = ClientConfig::new(uri).with_protocol(Protocol::Grpc);
///
/// let client = WorkerExecutionServiceClient::new(conn, config);
/// let response = client.claim_issue(request).await?;
/// ```
///
/// # Example (Connect / HTTP/1.1 or ALPN)
///
/// ```rust,ignore
/// use connectrpc::client::{HttpClient, ClientConfig};
///
/// let http = HttpClient::plaintext();  // cleartext http:// only
/// let config = ClientConfig::new("http://localhost:8080".parse()?);
///
/// let client = WorkerExecutionServiceClient::new(http, config);
/// let response = client.claim_issue(request).await?;
/// ```
///
/// # Working with the response
///
/// Unary calls return [`UnaryResponse<OwnedView<FooView>>`](::connectrpc::client::UnaryResponse).
/// [`view()`](::connectrpc::client::UnaryResponse::view) borrows the response
/// message, so field access is zero-copy:
///
/// ```rust,ignore
/// let resp = client.claim_issue(request).await?;
/// let name: &str = resp.view().name;  // borrow into the response buffer
/// ```
///
/// If you need the owned struct (e.g. to store or pass by value), use
/// [`into_owned()`](::connectrpc::client::UnaryResponse::into_owned):
///
/// ```rust,ignore
/// let owned = client.claim_issue(request).await?.into_owned();
/// ```
///
/// [`into_view()`](::connectrpc::client::UnaryResponse::into_view) keeps the
/// zero-copy decoded body (an `OwnedView`) without copying; field access on it
/// goes through `.reborrow()`. Streaming responses yield one
/// [`StreamMessage`](::connectrpc::StreamMessage) per received message from
/// `.message().await` — read fields zero-copy through the generated accessor
/// methods (`msg.name()`) or `.view()`, or convert with `.to_owned_message()`.
#[cfg(feature = "client")]
#[derive(Clone)]
pub struct WorkerExecutionServiceClient<T> {
    transport: T,
    config: ::connectrpc::client::ClientConfig,
}
#[cfg(feature = "client")]
impl<T> WorkerExecutionServiceClient<T>
where
    T: ::connectrpc::client::ClientTransport,
    <T::ResponseBody as ::connectrpc::http_body::Body>::Error: ::std::fmt::Display,
{
    /// Create a new client with the given transport and configuration.
    pub fn new(transport: T, config: ::connectrpc::client::ClientConfig) -> Self {
        Self { transport, config }
    }
    /// Get the client configuration.
    pub fn config(&self) -> &::connectrpc::client::ClientConfig {
        &self.config
    }
    /// Get a mutable reference to the client configuration.
    pub fn config_mut(&mut self) -> &mut ::connectrpc::client::ClientConfig {
        &mut self.config
    }
    /// Call the ClaimIssue RPC. Sends a request to /briar.worker.v1.WorkerExecutionService/ClaimIssue.
    pub async fn claim_issue(
        &self,
        request: crate::proto::briar::worker::v1::ClaimIssueRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::ClaimIssueResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.claim_issue_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the ClaimIssue RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn claim_issue_with_options(
        &self,
        request: crate::proto::briar::worker::v1::ClaimIssueRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::ClaimIssueResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                WORKER_EXECUTION_SERVICE_CLAIM_ISSUE_SPEC
                    .with_origin(::connectrpc::SpecOrigin::Client),
                request,
                options,
            )
            .await
    }
    /// Call the ListRunEvidence RPC. Sends a request to /briar.worker.v1.WorkerExecutionService/ListRunEvidence.
    pub async fn list_run_evidence(
        &self,
        request: crate::proto::briar::app::v1::ListRunEvidenceRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::app::v1::__buffa::view::ListRunEvidenceResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.list_run_evidence_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the ListRunEvidence RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn list_run_evidence_with_options(
        &self,
        request: crate::proto::briar::app::v1::ListRunEvidenceRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::app::v1::__buffa::view::ListRunEvidenceResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                WORKER_EXECUTION_SERVICE_LIST_RUN_EVIDENCE_SPEC
                    .with_origin(::connectrpc::SpecOrigin::Client),
                request,
                options,
            )
            .await
    }
    /// Call the AppendTranscriptEvents RPC. Sends a request to /briar.worker.v1.WorkerExecutionService/AppendTranscriptEvents.
    pub async fn append_transcript_events(
        &self,
        request: crate::proto::briar::worker::v1::AppendTranscriptEventsRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::AppendTranscriptEventsResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.append_transcript_events_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the AppendTranscriptEvents RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn append_transcript_events_with_options(
        &self,
        request: crate::proto::briar::worker::v1::AppendTranscriptEventsRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::AppendTranscriptEventsResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                WORKER_EXECUTION_SERVICE_APPEND_TRANSCRIPT_EVENTS_SPEC
                    .with_origin(::connectrpc::SpecOrigin::Client),
                request,
                options,
            )
            .await
    }
    /// Call the ReportIssueExecutionTelemetry RPC. Sends a request to /briar.worker.v1.WorkerExecutionService/ReportIssueExecutionTelemetry.
    pub async fn report_issue_execution_telemetry(
        &self,
        request: crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryRequest,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::ReportIssueExecutionTelemetryResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        self.report_issue_execution_telemetry_with_options(
                request,
                ::connectrpc::client::CallOptions::default(),
            )
            .await
    }
    /// Call the ReportIssueExecutionTelemetry RPC with explicit per-call options. Options override [`ClientConfig`](::connectrpc::client::ClientConfig) defaults.
    pub async fn report_issue_execution_telemetry_with_options(
        &self,
        request: crate::proto::briar::worker::v1::ReportIssueExecutionTelemetryRequest,
        options: ::connectrpc::client::CallOptions,
    ) -> Result<
        ::connectrpc::client::UnaryResponse<
            ::buffa::view::OwnedView<
                crate::proto::briar::worker::v1::__buffa::view::ReportIssueExecutionTelemetryResponseView<
                    'static,
                >,
            >,
        >,
        ::connectrpc::ConnectError,
    > {
        ::connectrpc::client::call_unary(
                &self.transport,
                &self.config,
                WORKER_EXECUTION_SERVICE_REPORT_ISSUE_EXECUTION_TELEMETRY_SPEC
                    .with_origin(::connectrpc::SpecOrigin::Client),
                request,
                options,
            )
            .await
    }
}
